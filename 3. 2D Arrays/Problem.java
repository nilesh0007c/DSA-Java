import java.util.*;
class Problem{

    // public static int rdfsa(int nums){
    //     int count = 0;
    //     int n = nums.length-1;
    //     for(int i=0; i<=n; i++){
    //         if(nums[i] == nums[i+1]){
    //             nums[i+1].remove();
    //             count++;
    //         }
    //     }
    //     return count;
    // }
  

   public static void main(String args[]) {
    int nums[] = {1,1,2,2,2,3,3,3,3,3,4,4,4};
    
    // // Handle empty array case
    // if (nums.length == 0) {
    //     System.out.print(0);
    //     return;
    // }

    // int uniqueIndex = 0; // Tracks where the next unique element goes

    // for (int i = 1; i < nums.length; i++) {
    //     // If we find a new, unique number
    //     if (nums[i] != nums[uniqueIndex]) {
    //         uniqueIndex++;
    //         nums[uniqueIndex] = nums[i]; // Move it forward
    //     }
    // }

    // // The number of unique elements is uniqueIndex + 1
    // int count = uniqueIndex + 1; 
    // System.out.print(count);
}

}